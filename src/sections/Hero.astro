---
import { getCollection } from "astro:content";
import BlogPrincipaleHome from "../components/BlogPrincipaleHome.astro";

const posts = await getCollection("posts");
---

{
  posts
    .reverse()
    .slice(0, 1)
    .map((post) => <BlogPrincipaleHome post={post} />)
}


